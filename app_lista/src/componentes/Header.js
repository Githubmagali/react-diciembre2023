import React from "react";

const Header = ()=>{
  return(
        <header>
            <h1>Lista de tareas</h1>
            <button>No mostrar</button>
        </header>
    );
}

export default Header;